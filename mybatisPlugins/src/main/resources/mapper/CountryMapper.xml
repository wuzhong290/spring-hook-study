<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.demo.mybatis.mapper.CountryMapper">
    <select id="selectById" resultType="com.demo.mybatis.model.Country">
      select * from country where id = #{id}
    </select>
    <select id="selectAll" resultType="com.demo.mybatis.model.Country">
      select * from country
    </select>
</mapper>

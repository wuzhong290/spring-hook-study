PropertyEditorRegistrySupport


org.springframework.beans.factory.config.CustomEditorConfigurer用于注册自定义PropertyEditorSupport
在此处进行debug看PropertyEditorSupport是否生效
    public void setCar(Car car) {
        this.car = car;
    }